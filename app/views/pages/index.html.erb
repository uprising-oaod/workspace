<h1>FKP Workspace. Structure.</h1>
<ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.title, page %>
    </li>
  <% end %>
</ul>

<%= link_to "New Page for Structure", new_page_path %>